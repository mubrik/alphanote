container_commands:
    01_chmod:
        command: "chmod +x .platform/hooks/postdeploy/01_django_migrate.sh"

option_settings:
    aws:elasticbeanstalk:application:environment:
      DJANGO_SETTINGS_MODULE: alphanote.settings
      LOG_LEVEL: INFO
      LOG_FILE_PATH: /tmp/django-app.log
    aws:elasticbeanstalk:container:python:
        WSGIPath: alphanote.wsgi:application
